'use client';

/**
 * Kiosk Start Page
 * This page exists solely to satisfy Next.js routing.
 * The actual UI is fully controlled by KioskViewController in the layout.
 * All screen transitions happen in-memory via kioskStore — zero page reloads.
 */
export default function KioskStartPage() {
    return null;
}
